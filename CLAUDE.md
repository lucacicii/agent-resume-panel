# Claude Code Project Router

Follow [AGENTS.md](AGENTS.md). Use `.agents/menus-index.md` only when the task describes a product area or user-visible feature without a concrete path or searchable identifier. Load a single matching `.agents/menus/*.md` detail file, plus only the relevant `.agents/extended/` reference.
