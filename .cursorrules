Follow AGENTS.md. For feature requests without a concrete path or searchable identifier, consult .agents/menus-index.md and then only the matching .agents/menus detail file. Load .agents/extended references only when their subject is relevant.
